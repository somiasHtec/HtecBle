import { StyleSheet } from 'react-native';

import { COLORS } from '../../assets';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  label: {
    color: COLORS.grey,
    fontSize: 16,
    marginRight: 15,
  },
});
